// Code generated by testkit bench. DO NOT EDIT.
// Source: hasher.go:103-166 (testkit bench Hasher)

package cryptotest

import (
	"context"
	"testing"

	"go.thesmos.sh/core/crypto"
	"go.thesmos.sh/testkit/bench"
	"go.thesmos.sh/testkit/bindings"
)

// BenchmarkHasherContract runs performance benchmarks against
// implementations of [crypto.Hasher] produced by factory.
//
//	Default benchmarks: 7 hot-path measurements across 7 methods
//	Shapes benchmarked: Pure (Algorithm, Combine, CombineTagged, Hash, HashTagged, ID, NewStream)
//	Plug-in points:     HasherBenchOnAlgorithm, HasherBenchOnCombine, HasherBenchOnCombineTagged, HasherBenchOnHash, HasherBenchOnHashTagged, HasherBenchOnID, HasherBenchOnNewStream, HasherBenchCustom
func BenchmarkHasherContract(
	b *testing.B,
	factory func() crypto.Hasher,
	opts ...HasherBenchOption,
) {
	b.Helper()
	cfg := newHasherBenchConfig(opts...)

	benchHasherAlgorithm(b, factory, &cfg)
	benchHasherCombine(b, factory, &cfg)
	benchHasherCombineTagged(b, factory, &cfg)
	benchHasherHash(b, factory, &cfg)
	benchHasherHashTagged(b, factory, &cfg)
	benchHasherID(b, factory, &cfg)
	benchHasherNewStream(b, factory, &cfg)

	for _, custom := range cfg.custom {
		b.Run(custom.name, func(b *testing.B) {
			custom.fn(b, factory())
		})
	}
}

func benchHasherAlgorithm(b *testing.B, factory func() crypto.Hasher, cfg *hasherBenchConfig) {
	b.Helper()
	b.Run("Algorithm/hot-path", func(b *testing.B) {
		defer func() {
			if r := recover(); r != nil {
				b.Skipf("hot-path: Algorithm panicked on zero-value args (%v) — supply sample values via HasherBenchOnAlgorithm", r)
			}
		}()
		impl := factory()
		if cfg.prePopulate != nil {
			cfg.prePopulate(b.Context(), impl)
		}
		b.ResetTimer()
		b.ReportAllocs()
		for b.Loop() {
			_ = impl.Algorithm()
		}
	})
	if len(cfg.onAlgorithm) > 0 {
		b.Run("Algorithm", func(b *testing.B) {
			prePopFactory := func() crypto.Hasher {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(b.Context(), impl)
				}
				return impl
			}
			pctx := bench.PureContext[crypto.Hasher, crypto.Algorithm]{
				B: b,
				PureBindings: bindings.PureBindings[crypto.Hasher, crypto.Algorithm]{
					Factory: prePopFactory,
					Call: func(impl crypto.Hasher) crypto.Algorithm {
						return impl.Algorithm()
					},
				},
			}
			for _, a := range cfg.onAlgorithm {
				a(pctx)
			}
		})
	}
}

func benchHasherCombine(b *testing.B, factory func() crypto.Hasher, cfg *hasherBenchConfig) {
	b.Helper()
	b.Run("Combine/hot-path", func(b *testing.B) {
		defer func() {
			if r := recover(); r != nil {
				b.Skipf("hot-path: Combine panicked on zero-value args (%v) — supply sample values via HasherBenchOnCombine", r)
			}
		}()
		impl := factory()
		if cfg.prePopulate != nil {
			cfg.prePopulate(b.Context(), impl)
		}
		b.ResetTimer()
		b.ReportAllocs()
		for b.Loop() {
			_ = impl.Combine(SampleDigest(impl), SampleDigest(impl))
		}
	})
	if len(cfg.onCombine) > 0 {
		b.Run("Combine", func(b *testing.B) {
			prePopFactory := func() crypto.Hasher {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(b.Context(), impl)
				}
				return impl
			}
			pctx := bench.PureContext[crypto.Hasher, crypto.Digest]{
				B: b,
				PureBindings: bindings.PureBindings[crypto.Hasher, crypto.Digest]{
					Factory: prePopFactory,
					Call: func(impl crypto.Hasher) crypto.Digest {
						return impl.Combine(SampleDigest(impl), SampleDigest(impl))
					},
				},
			}
			for _, a := range cfg.onCombine {
				a(pctx)
			}
		})
	}
}

func benchHasherCombineTagged(b *testing.B, factory func() crypto.Hasher, cfg *hasherBenchConfig) {
	b.Helper()
	b.Run("CombineTagged/hot-path", func(b *testing.B) {
		defer func() {
			if r := recover(); r != nil {
				b.Skipf("hot-path: CombineTagged panicked on zero-value args (%v) — supply sample values via HasherBenchOnCombineTagged", r)
			}
		}()
		impl := factory()
		if cfg.prePopulate != nil {
			cfg.prePopulate(b.Context(), impl)
		}
		b.ResetTimer()
		b.ReportAllocs()
		for b.Loop() {
			_ = impl.CombineTagged(SampleBinaryRole(impl), SampleDigest(impl), SampleDigest(impl))
		}
	})
	if len(cfg.onCombineTagged) > 0 {
		b.Run("CombineTagged", func(b *testing.B) {
			prePopFactory := func() crypto.Hasher {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(b.Context(), impl)
				}
				return impl
			}
			pctx := bench.PureContext[crypto.Hasher, crypto.Digest]{
				B: b,
				PureBindings: bindings.PureBindings[crypto.Hasher, crypto.Digest]{
					Factory: prePopFactory,
					Call: func(impl crypto.Hasher) crypto.Digest {
						return impl.CombineTagged(SampleBinaryRole(impl), SampleDigest(impl), SampleDigest(impl))
					},
				},
			}
			for _, a := range cfg.onCombineTagged {
				a(pctx)
			}
		})
	}
}

func benchHasherHash(b *testing.B, factory func() crypto.Hasher, cfg *hasherBenchConfig) {
	b.Helper()
	b.Run("Hash/hot-path", func(b *testing.B) {
		defer func() {
			if r := recover(); r != nil {
				b.Skipf("hot-path: Hash panicked on zero-value args (%v) — supply sample values via HasherBenchOnHash", r)
			}
		}()
		impl := factory()
		if cfg.prePopulate != nil {
			cfg.prePopulate(b.Context(), impl)
		}
		b.ResetTimer()
		b.ReportAllocs()
		for b.Loop() {
			_ = impl.Hash(nil)
		}
	})
	if len(cfg.onHash) > 0 {
		b.Run("Hash", func(b *testing.B) {
			prePopFactory := func() crypto.Hasher {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(b.Context(), impl)
				}
				return impl
			}
			pctx := bench.PureContext[crypto.Hasher, crypto.Digest]{
				B: b,
				PureBindings: bindings.PureBindings[crypto.Hasher, crypto.Digest]{
					Factory: prePopFactory,
					Call: func(impl crypto.Hasher) crypto.Digest {
						return impl.Hash(nil)
					},
				},
			}
			for _, a := range cfg.onHash {
				a(pctx)
			}
		})
	}
}

func benchHasherHashTagged(b *testing.B, factory func() crypto.Hasher, cfg *hasherBenchConfig) {
	b.Helper()
	b.Run("HashTagged/hot-path", func(b *testing.B) {
		defer func() {
			if r := recover(); r != nil {
				b.Skipf("hot-path: HashTagged panicked on zero-value args (%v) — supply sample values via HasherBenchOnHashTagged", r)
			}
		}()
		impl := factory()
		if cfg.prePopulate != nil {
			cfg.prePopulate(b.Context(), impl)
		}
		b.ResetTimer()
		b.ReportAllocs()
		for b.Loop() {
			_ = impl.HashTagged(SampleUnaryRole(impl), SampleBytes(impl))
		}
	})
	if len(cfg.onHashTagged) > 0 {
		b.Run("HashTagged", func(b *testing.B) {
			prePopFactory := func() crypto.Hasher {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(b.Context(), impl)
				}
				return impl
			}
			pctx := bench.PureContext[crypto.Hasher, crypto.Digest]{
				B: b,
				PureBindings: bindings.PureBindings[crypto.Hasher, crypto.Digest]{
					Factory: prePopFactory,
					Call: func(impl crypto.Hasher) crypto.Digest {
						return impl.HashTagged(SampleUnaryRole(impl), SampleBytes(impl))
					},
				},
			}
			for _, a := range cfg.onHashTagged {
				a(pctx)
			}
		})
	}
}

func benchHasherID(b *testing.B, factory func() crypto.Hasher, cfg *hasherBenchConfig) {
	b.Helper()
	b.Run("ID/hot-path", func(b *testing.B) {
		defer func() {
			if r := recover(); r != nil {
				b.Skipf("hot-path: ID panicked on zero-value args (%v) — supply sample values via HasherBenchOnID", r)
			}
		}()
		impl := factory()
		if cfg.prePopulate != nil {
			cfg.prePopulate(b.Context(), impl)
		}
		b.ResetTimer()
		b.ReportAllocs()
		for b.Loop() {
			_ = impl.ID()
		}
	})
	if len(cfg.onID) > 0 {
		b.Run("ID", func(b *testing.B) {
			prePopFactory := func() crypto.Hasher {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(b.Context(), impl)
				}
				return impl
			}
			pctx := bench.PureContext[crypto.Hasher, crypto.ID]{
				B: b,
				PureBindings: bindings.PureBindings[crypto.Hasher, crypto.ID]{
					Factory: prePopFactory,
					Call: func(impl crypto.Hasher) crypto.ID {
						return impl.ID()
					},
				},
			}
			for _, a := range cfg.onID {
				a(pctx)
			}
		})
	}
}

func benchHasherNewStream(b *testing.B, factory func() crypto.Hasher, cfg *hasherBenchConfig) {
	b.Helper()
	b.Run("NewStream/hot-path", func(b *testing.B) {
		defer func() {
			if r := recover(); r != nil {
				b.Skipf("hot-path: NewStream panicked on zero-value args (%v) — supply sample values via HasherBenchOnNewStream", r)
			}
		}()
		impl := factory()
		if cfg.prePopulate != nil {
			cfg.prePopulate(b.Context(), impl)
		}
		b.ResetTimer()
		b.ReportAllocs()
		for b.Loop() {
			_ = impl.NewStream()
		}
	})
	if len(cfg.onNewStream) > 0 {
		b.Run("NewStream", func(b *testing.B) {
			prePopFactory := func() crypto.Hasher {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(b.Context(), impl)
				}
				return impl
			}
			pctx := bench.PureContext[crypto.Hasher, crypto.Stream]{
				B: b,
				PureBindings: bindings.PureBindings[crypto.Hasher, crypto.Stream]{
					Factory: prePopFactory,
					Call: func(impl crypto.Hasher) crypto.Stream {
						return impl.NewStream()
					},
				},
			}
			for _, a := range cfg.onNewStream {
				a(pctx)
			}
		})
	}
}

// HasherBenchOption configures [BenchmarkHasherContract].
type HasherBenchOption func(*hasherBenchConfig)

// HasherBenchPrePopulate runs before each benchmark against a fresh impl.
func HasherBenchPrePopulate(fn func(ctx context.Context, s crypto.Hasher)) HasherBenchOption {
	return func(c *hasherBenchConfig) { c.prePopulate = fn }
}

// HasherBenchCustom adds a free-form sub-benchmark.
func HasherBenchCustom(name string, fn func(*testing.B, crypto.Hasher)) HasherBenchOption {
	return func(c *hasherBenchConfig) {
		c.custom = append(c.custom, hasherBenchCustomSub{name: name, fn: fn})
	}
}

// HasherBenchOnAlgorithm adds plug-in bench primitives for the Algorithm method.
func HasherBenchOnAlgorithm(assertions ...bench.Pure[crypto.Hasher, crypto.Algorithm]) HasherBenchOption {
	return func(c *hasherBenchConfig) {
		c.onAlgorithm = append(c.onAlgorithm, assertions...)
	}
}

// HasherBenchOnCombine adds plug-in bench primitives for the Combine method.
func HasherBenchOnCombine(assertions ...bench.Pure[crypto.Hasher, crypto.Digest]) HasherBenchOption {
	return func(c *hasherBenchConfig) {
		c.onCombine = append(c.onCombine, assertions...)
	}
}

// HasherBenchOnCombineTagged adds plug-in bench primitives for the CombineTagged method.
func HasherBenchOnCombineTagged(assertions ...bench.Pure[crypto.Hasher, crypto.Digest]) HasherBenchOption {
	return func(c *hasherBenchConfig) {
		c.onCombineTagged = append(c.onCombineTagged, assertions...)
	}
}

// HasherBenchOnHash adds plug-in bench primitives for the Hash method.
func HasherBenchOnHash(assertions ...bench.Pure[crypto.Hasher, crypto.Digest]) HasherBenchOption {
	return func(c *hasherBenchConfig) {
		c.onHash = append(c.onHash, assertions...)
	}
}

// HasherBenchOnHashTagged adds plug-in bench primitives for the HashTagged method.
func HasherBenchOnHashTagged(assertions ...bench.Pure[crypto.Hasher, crypto.Digest]) HasherBenchOption {
	return func(c *hasherBenchConfig) {
		c.onHashTagged = append(c.onHashTagged, assertions...)
	}
}

// HasherBenchOnID adds plug-in bench primitives for the ID method.
func HasherBenchOnID(assertions ...bench.Pure[crypto.Hasher, crypto.ID]) HasherBenchOption {
	return func(c *hasherBenchConfig) {
		c.onID = append(c.onID, assertions...)
	}
}

// HasherBenchOnNewStream adds plug-in bench primitives for the NewStream method.
func HasherBenchOnNewStream(assertions ...bench.Pure[crypto.Hasher, crypto.Stream]) HasherBenchOption {
	return func(c *hasherBenchConfig) {
		c.onNewStream = append(c.onNewStream, assertions...)
	}
}

type hasherBenchCustomSub struct {
	name string
	fn   func(*testing.B, crypto.Hasher)
}

type hasherBenchConfig struct {
	prePopulate     func(context.Context, crypto.Hasher)
	custom          []hasherBenchCustomSub
	onAlgorithm     []bench.Pure[crypto.Hasher, crypto.Algorithm]
	onCombine       []bench.Pure[crypto.Hasher, crypto.Digest]
	onCombineTagged []bench.Pure[crypto.Hasher, crypto.Digest]
	onHash          []bench.Pure[crypto.Hasher, crypto.Digest]
	onHashTagged    []bench.Pure[crypto.Hasher, crypto.Digest]
	onID            []bench.Pure[crypto.Hasher, crypto.ID]
	onNewStream     []bench.Pure[crypto.Hasher, crypto.Stream]
}

func newHasherBenchConfig(opts ...HasherBenchOption) hasherBenchConfig {
	var cfg hasherBenchConfig
	for _, opt := range opts {
		opt(&cfg)
	}
	return cfg
}
